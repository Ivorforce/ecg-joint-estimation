# The exact versions the paper's results were computed with (Python 3.9, macOS arm64).
# With these, the fits are bit-identical to the paper's.
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.6.1
PyWavelets==1.6.0
tensorflow==2.16.2
keras==3.8.0
wfdb==4.3.0
matplotlib==3.9.4  # the paper's Fig. 2 was rendered with 3.11.1; 3.9.4 moves the layout by a pixel
